# ⚡ 电力世界 · PowerWorld

一款**以电网本身为主角**的硬核建造经营游戏（MVP 垂直切片）。

你不是简单地放一个电厂、看一个"供电够不够"的数字——你经营的是一张**活的电网**：
潮流按线路阻抗真实分配、线路有热极限会过载跳闸并**连锁停电**、机组无法瞬时响应、
新能源靠天吃饭、频率随供需失衡波动、燃料与碳排放压着现金流。

**Phase 2 已加入**：高压/中压两级电网与**变电站变压器过载保护**、**储能电池**（充放电消纳新能源）、
**天气与危机事件**（热浪/寒潮/无风/阴雨/风暴损线）、**三关战役 + 存档读档**。

> 核心玩法循环：**调度发电 → 直流潮流求解 → 频率/拥堵 → 跳闸保护 → 经济结算**

## 快速开始

```bash
npm install
npm run dev      # 启动开发服务器（默认 http://localhost:5173）
npm run test     # 运行潮流求解器单元测试
npm run build    # 类型检查 + 生产打包到 dist/
```

## 怎么玩

进入游戏先在**主菜单选择关卡**（或「继续上次存档」）。关卡开始处于**暂停**状态，先布网再开跑：

1. 强制拓扑：**电厂 →[高压]→ 变电站 →[中压]→ 负荷**。所以要先建**变电站**做枢纽，再拉线。
2. 备足电源（燃煤=便宜基荷但爬坡慢、燃气=灵活调峰但贵、风/光=零燃料但间歇、核电=巨大基荷），
   必要时加**储能**填爬坡缺口、消纳新能源、平滑峰谷。
3. 点 **▶**（或空格）开始推进时间。
4. 盯住顶栏：**频率**偏离 50Hz 说明供需失衡；线路变**红**接近热极限、持续过载会**跳闸**；
   **变电站**通过量超额定会**变压器跳闸**切断下游；留意**天气**栏的热浪/风暴等冲击。
5. 目标随关卡而定（如撑到第 12 天且可靠性≥92%），同时别破产。

**操作**：鼠标拖拽平移 · 滚轮缩放 · 数字键 1-9 切换工具 · 空格暂停/继续 · Esc 取消连线 ·
用「检查/重合闸」点击跳闸的**线路或变电站**可恢复送电 · 顶栏 💾 存档、☰ 返回菜单。

## 架构（仿真与渲染彻底解耦）

```
src/
  sim/                # 纯逻辑仿真核心（无渲染依赖，可单元测试）
    types.ts          # 领域类型
    powerflow.ts      # 直流潮流（DC Power Flow）求解器 + 单测
    grid.ts           # 电网图容器 + 孤岛分析 + 电压等级 + 序列化
    profiles.ts       # 日内用电曲线 + 新能源天气模型
    events.ts         # 天气与危机事件系统
    simulation.ts     # 主循环：调度/储能/潮流/频率/跳闸/事件/经济/存档
  render/
    renderer.ts       # PixiJS 渲染：母线、线路按电压/负载着色、潮流粒子、相机
  ui/
    hud.ts            # DOM 状态栏/工具栏/日志/检查器/结束遮罩
    menu.ts           # 主菜单 / 关卡选择
  game/
    scenarios.ts      # 战役关卡定义
    save.ts           # localStorage 存档读写
  config/
    components.ts     # 机组/电压/储能目录 + 全部可调平衡常量
  main.ts             # 组装与交互
```

**设计铁律**：`sim/` 是确定性、可测试的模型；`render/` 与 `ui/` 只读状态、发指令。
这让我们能单测、跑无头平衡测试、未来替换渲染器或接入自动调度 AI。

## 硬核仿真要点

- **直流潮流**：`P = B·θ`，潮流按 `1/X` 分配，真实产生拥堵与连锁跳闸（见 `powerflow.test.ts`）。
- **经济调度**：可调机组按边际成本 merit-order 出力，受**爬坡率**约束 → "机组无法瞬时响应"的张力。
- **频率**：由供需失衡推算，过低触发甩负荷（停电）。
- **孤岛**：线路跳闸后电网分裂，无电源的孤岛整片停电。
- **线损**：`loss ≈ R·I²`，远距离输电要付出代价（这也是要升压输电的动机）。
- **经济**：上网电价收入 − 燃料 − 碳成本 − 失负荷罚款；碳价逐日上涨制造减碳压力。

## 路线图

- **Phase 1 (MVP)** ✅：核心循环 + 三类机组 + 潮流/频率/跳闸/经济。
- **Phase 2** ✅：电压等级(HV/MV) + 变电站变压器保护、储能电池、天气/危机事件、三关战役 + 存档。
- **Phase 3**：科技树、N-1 冗余校核、减碳/污染、需求响应/智能电网。
- **Phase 4**：打磨、音效、教程、沙盒、Mod。

平衡数值全部集中在 `src/config/components.ts`，方便调参。
